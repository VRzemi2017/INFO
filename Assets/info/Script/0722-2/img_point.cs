using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class img_point : MonoBehaviour {
	GameObject Mgr;
	// Use this for initialization
	void Start () {
		/*Mgr = GameObject.Find( "Info_Mgr" );
		transform.position = Mgr.transform.position;
		Vector3 angle = Mgr.transform.position;
		angle.y = 90;
		transform.rotation = Quaternion.Euler( angle );
		Vector3 angle = new Vector3( 0, 0, 0 );
		angle.y = Mgr.transform.localRotation.eulerAngles.y;
		transform.LookAt (angle);*/
	}
	
	// Update is called once per frame
	void Update () {
    }
}
